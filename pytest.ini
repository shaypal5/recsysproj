[pytest]
testpaths =
    tests
    recasino
norecursedirs=dist build .tox
markers=
    log
    onehotencode
    first
    schematize
    bound_col
addopts =
    --doctest-modules
    --cov=recasino
    -rxXs
    --ignore-glob="*bound_column_potential.py"
